- Fix the issues where in port at key and output at shuffle creates issues (the problem lies in the way IP files are generated)
- Modify the framework so it works with two different bolts as input to a stage
- Fix the problem with new-nyc application

Issues:
Fix the issue with Wordcount and buffer size > 0
Fix the issue with  SpikeDetection and buffer size == 1000 

